name: Build & Deploy to Docker Swarm

on:
  push:
    branches: [ "dev" ]  # ← 실제 배포 브랜치 기준

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  ORG: career-hy  # ← GHCR은 반드시 소문자만 허용하므로 하드코딩
  BACKEND_SERVICE: rag_backend
  LLM_SERVICE: rag_llm-service
  INGEST_SERVICE: rag_ingestion

jobs:
  build:
    name: Build & Push Images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - name: backend
            context: ./backend
            dockerfile: ./backend/Dockerfile
          - name: llm-service
            context: ./llm-service
            dockerfile: ./llm-service/Dockerfile
          - name: ingestion
            context: ./ingestion
            dockerfile: ./ingestion/Dockerfile

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & Push ${{ matrix.name }}
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.ORG }}/${{ matrix.name }}:dev
            ${{ env.REGISTRY }}/${{ env.ORG }}/${{ matrix.name }}:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Swarm
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Write SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ci_key
          chmod 600 ~/.ssh/ci_key
          ssh-keyscan -H ${{ secrets.EC2_HOST_A }} >> ~/.ssh/known_hosts

      - name: Deploy Backend
        env:
          HOST: ${{ secrets.EC2_HOST_A }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/ci_key ${USER}@${HOST} \
            "docker service update --image ghcr.io/${{ env.ORG }}/backend:dev --update-order stop-first $BACKEND_SERVICE || \
             docker service create --name $BACKEND_SERVICE --publish 5001:5001 ghcr.io/${{ env.ORG }}/backend:dev"

      - name: Deploy LLM-Service
        env:
          HOST: ${{ secrets.EC2_HOST_A }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/ci_key ${USER}@${HOST} \
            "docker service update --image ghcr.io/${{ env.ORG }}/llm-service:dev --update-order stop-first $LLM_SERVICE || \
             docker service create --name $LLM_SERVICE ghcr.io/${{ env.ORG }}/llm-service:dev"

      - name: Deploy Ingestion
        env:
          HOST: ${{ secrets.EC2_HOST_A }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/ci_key ${USER}@${HOST} \
            "docker service update --image ghcr.io/${{ env.ORG }}/ingestion:dev --update-order stop-first $INGEST_SERVICE || \
             docker service create --name $INGEST_SERVICE ghcr.io/${{ env.ORG }}/ingestion:dev"

      - name: Check Swarm status
        env:
          HOST: ${{ secrets.EC2_HOST_A }}
          USER: ${{ secrets.EC2_USER }}
        run: |
          ssh -i ~/.ssh/ci_key ${USER}@${HOST} "docker service ls && docker node ls"
