# YAML 설정 읽기
PyYAML

# .env 파일 환경변수 로드
python-dotenv

# PDF → 텍스트
pypdf
PyMuPDF==1.23.6

#임베딩 모델 
# 빌드할 때 GPU 사용 관련해서 문제가 있어서 당장 사용하지 않는 임베딩 모델들 주석처리 하겠습니다.
# sentence-transformers
# transformers
# scikit-learn

# LangChain 코어
langchain==0.1.20
langchain-openai==0.1.7
openai==1.24.0
httpx==0.26.0

# LangChain Community Toolkit
numpy
langchain-community==0.0.38
# langchain-teddynote  # 주석처리: 복잡한 의존성으로 인한 빌드 문제

# 벡터 DB (Chroma) - 버전 고정으로 telemetry 호환성 보장
chromadb==0.4.22
# PostHog 버전 고정 - ChromaDB 0.4.22와 호환성 문제 해결 (posthog>=6.0.0에서 capture() 시그니처 변경)
posthog==4.8.0

# 진행률 표시
tqdm

#토큰 수 확인 
tiktoken

# fast api
fastapi>=0.68.0
uvicorn>=0.15.0
pydantic>=1.8.0

# s3
boto3